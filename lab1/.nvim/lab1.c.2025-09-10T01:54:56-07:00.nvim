#define _POSIX_C_SOURCE 200809L
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
int main() {
  char *lineptr = NULL, *saveptr1, saveptr2, token, subtoken;
  size_t n = 0;
  ssize_t nread;
  ssize_t deliminator;
  char delim = ' ';
  nread = getline(&lineptr, &n, stdin);
  // fwrite(lineptr, nread, 1, stdout);
  token = strtok_r(&lineptr, &delim, *saveptr);

  free(lineptr);

  return 0;
}
