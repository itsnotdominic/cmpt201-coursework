#define _POSIX_C_SOURCE 200809L
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
int main() {
  char *lineptr = NULL;
  size_t n = 0;
  ssize_t nread;
  ssize_t deliminator;
  
  nread = getline(&lineptr, &n, stdin);
  deliminator = getdelim(&lineptr,&n,' ',stdin);
  //fwrite(lineptr, nread, 1, stdout);
  char* strtok_r(&lineptr, &delim, *saveptr);
  


  free(lineptr);


  return 0;
}
